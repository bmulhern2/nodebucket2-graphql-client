import React, { useState, useEffect } from 'react';

const EditTask = ({ id }) => {
    return (
        <div className="modal">
            <div>{console.log(id)}</div>
        </div>
    );
};

export default EditTask;