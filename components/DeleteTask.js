import React, { useEffect, useState } from 'react';

const DeleteTask = ({ id }) => {
    return (
        <div className="modal">
            <div>{console.log(id)}</div>
        </div>
    );
};

export default DeleteTask;