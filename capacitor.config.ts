import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nodebucket2.app',
  appName: 'nodebucket2',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
